const statusRoot = "/webbox-assets/status";

export const uiAssets = {
  empty: `${statusRoot}/empty.svg`,
  emptyMessage: `${statusRoot}/empty_msg.svg`,
  loading: `${statusRoot}/loading.png`,
  success: `${statusRoot}/succeed.png`,
  warning: `${statusRoot}/warning.png`,
  error: `${statusRoot}/error.png`
} as const;
